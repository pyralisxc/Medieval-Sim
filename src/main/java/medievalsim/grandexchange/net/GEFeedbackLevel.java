package medievalsim.grandexchange.net;

/**
 * Severity associated with a feedback message so the UI can pick
 * colors or icons that match the context rather than relying on
 * ad-hoc error booleans.
 */
public enum GEFeedbackLevel {
    INFO,
    WARN,
    ERROR
}
